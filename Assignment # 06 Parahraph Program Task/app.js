// Wrire a program in JavaScript that will take a user input. User input should be string must.
// User can be write anything in upper or lower case but everything should be print in title case.

// Solution :
// function titleCase(val) {
//     var getUserValue = val.split(" ");
//     var userLength = getUserValue.length;

//     for (var i = 0; i < userLength; i++) {
//         getUserValue[i] = getUserValue[i][0].toUpperCase() + getUserValue[i].slice(1).toLowerCase();
//     }
//     var convertArrToStr = getUserValue.join(" ");
//     return convertArrToStr;
// }

// var userInput = prompt("What is your full name ?");
// var result = titleCase(userInput);
// console.log(result);

// DONE.........!!!